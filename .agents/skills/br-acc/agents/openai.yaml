interface:
  display_name: "Br Acc"
  short_description: "Repo-specific patterns and workflows for br-acc"
  default_prompt: "Use the br-acc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true